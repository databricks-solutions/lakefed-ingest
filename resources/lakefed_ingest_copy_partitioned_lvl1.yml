resources:
  jobs:
    lakefed_ingest_copy_partitioned_lvl1:
      name: "lakefed_ingest_copy_partitioned_lvl1"
      description: "Performs a partitioned copy using parameters returned from get_task"
      tasks:
        - task_key: get_task
          description: "Returns parameters for the task_id provided as a job parameter"
          sql_task:
            file:
              path: ../src/lakefed_ingest/get_task.sql
            warehouse_id: ${var.warehouse_id}
        - task_key: get_src_tbl_metadata
          depends_on:
            - task_key: get_task
          sql_task:
            file:
              path: ../src/lakefed_ingest/get_src_tbl_metadata.sql
            parameters:
              src_catalog: "{{tasks.get_task.output.first_row.src_catalog}}"
              src_schema: "{{tasks.get_task.output.first_row.src_schema}}"
              src_table: "{{tasks.get_task.output.first_row.src_table}}"
              select_list: "{{tasks.get_task.output.first_row.select_list}}"
            warehouse_id: ${var.warehouse_id}
        - task_key: create_sink_table
          depends_on:
            - task_key: get_src_tbl_metadata
          notebook_task:
            notebook_path: ../src/lakefed_ingest/create_sink_table.ipynb
            base_parameters:
              src_catalog: "{{tasks.get_task.output.first_row.src_catalog}}"
              src_schema: "{{tasks.get_task.output.first_row.src_schema}}"
              src_table: "{{tasks.get_task.output.first_row.src_table}}"
              select_list: "{{tasks.get_task.output.first_row.select_list}}"
              tgt_catalog: "{{tasks.get_task.output.first_row.sink_catalog}}"
              tgt_schema: "{{tasks.get_task.output.first_row.sink_schema}}"
              tgt_table: "{{tasks.get_task.output.first_row.sink_table}}"
              load_partitioned: "{{tasks.get_task.output.first_row.load_partitioned}}"
              enable_iceberg_reads: "{{tasks.get_task.output.first_row.enable_iceberg_reads}}"
              sink_cluster_cols: "{{tasks.get_task.output.first_row.sink_cluster_cols}}"
              src_tbl_metadata: "{{tasks.get_src_tbl_metadata.output.first_row.json_metadata}}"
            source: WORKSPACE
            warehouse_id: ${var.warehouse_id}
        - task_key: create_partition_udtf
          depends_on:
            - task_key: create_sink_table
          sql_task:
            file:
              path: ../src/lakefed_ingest/create_partition_udtf.sql
            parameters:
              ctrl_catalog: "{{job.parameters.ctrl_catalog}}"
              ctrl_schema: "{{job.parameters.ctrl_schema}}"
            warehouse_id: ${var.warehouse_id}
          description: Creates generate_partition_list UDTF in ctrl catalog/schema
        - task_key: generate_partitions
          depends_on:
            - task_key: create_partition_udtf
          sql_task:
            file:
              path: ../src/lakefed_ingest/generate_partitions.sql
            parameters:
              partition_col: "{{tasks.get_task.output.first_row.partition_col}}"
              partition_size_mb: "{{tasks.get_task.output.first_row.partition_size_mb}}"
              src_catalog: "{{tasks.get_task.output.first_row.src_catalog}}"
              src_schema: "{{tasks.get_task.output.first_row.src_schema}}"
              src_table: "{{tasks.get_task.output.first_row.src_table}}"
              src_type: "{{tasks.get_task.output.first_row.src_type}}"
              tgt_catalog: "{{tasks.get_task.output.first_row.sink_catalog}}"
              tgt_schema: "{{tasks.get_task.output.first_row.sink_schema}}"
              tgt_table: "{{tasks.get_task.output.first_row.sink_table}}"
              src_connection: "{{tasks.get_task.output.first_row.src_connection}}"
              src_database: "{{tasks.get_task.output.first_row.src_database}}"
              ctrl_catalog: "{{job.parameters.ctrl_catalog}}"
              ctrl_schema: "{{job.parameters.ctrl_schema}}"
            warehouse_id: ${var.warehouse_id}
          description: Generates partition WHERE clauses and writes _partitions table
        - task_key: foreach_batch
          depends_on:
            - task_key: generate_partitions
          for_each_task:
            inputs: "{{tasks.generate_partitions.output.first_row.batch_id_list}}"
            concurrency: 1
            task:
              task_key: copy_batch
              run_job_task:
                job_id: ${resources.jobs.lakefed_ingest_copy_partitioned_lvl2.id}
                job_parameters:
                  src_schema: "{{tasks.get_task.output.first_row.src_schema}}"
                  src_catalog: "{{tasks.get_task.output.first_row.src_catalog}}"
                  src_table: "{{tasks.get_task.output.first_row.src_table}}"
                  select_list: "{{tasks.get_task.output.first_row.select_list}}"
                  tgt_catalog: "{{tasks.get_task.output.first_row.sink_catalog}}"
                  tgt_schema: "{{tasks.get_task.output.first_row.sink_schema}}"
                  tgt_table: "{{tasks.get_task.output.first_row.sink_table}}"
                  batch_id: "{{input}}"
              description: Copies data based on provided parameters
          description: Iterates id_list set in get_partitions task
      tags:
        business_unit: ""
        project: lakefed_ingest
      queue:
        enabled: false
      parameters:
        - name: task_id
          default: "1"
        - name: ctrl_catalog
          default: "lakefed_ingest"
        - name: ctrl_schema
          default: "default"
        - name: ctrl_table
          default: "control"